using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnapObject : MonoBehaviour
{
    //Rerefence the snap zone collider we'll be using
   // public GameObject SnapLocation;

    //Reference the game object that the snapping objects will become a part of
  //  public GameObject

    //Create a variable that will be used by the RocketLauch script to determine if all three pieces are in place
    //public bool isSnapped;

    //bollean variable use to reference the"snapped" boolean from the SnapToLocation script
    //private bool objectSnapped;

    //bollean variable to determine if the object is currently being held by the player
    //private bool grabbed;

    // Start is called before the first frame update
    //void Start()
  //  {
        
    //}

    // Update is called once per frame
    //void Update()
   // {
        //set grabbed to equal the bollean value "isGrabbe" from the OVRGrabble script
        //grabbed = GetComponent<>().isGrabbed;

        //set objectSnapped equal to the snapped boolean from  Sna
    //}
}
